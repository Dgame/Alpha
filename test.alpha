main() {
	print " :: Base test:"
	print 42
	print 23

	a = 42
	b = a

	print " :: First variables:"
	print a
	print b

	a = 4
	b = 2

	print " :: Reassigned variables:"
	print a
	print b

	print " :: Math. prints:"
	print 2 + 2 + 2 + 2
	print 2 - 2 - 2 - 2
	print 2 * 2
	print 25 + 5
	print 1 + 2 * 3

	print " :: Math. variables:"
	c = 1 + 2 * 3
	d = (1 + 2) * 3
	e = 2 - 3 * 2
	f = (2 - 3) * 2

	print c
	print d
	print e
	print f

	print " :: Math. prints with variables:"
	print c + d
	print d + c

	print " :: Math. with variables and stored into variables:"

	c = c + d

	print " > c is now ", c
	print " > But d is still ", d

	print " :: Pointer Arithmetik:"
	g -> b
	h <- g

	print g
	print h

	print "Hallo,", "Welt"

	print " :: If Test ohne else"

	if (a) {
		print "A ist valide"
	}

	a = 0

	if (a) {
		print "A ist immer noch valide"
	}

	if (a == 0) {
		print "A ist 0"
	}

	a = a + 1
	a = a * 2

	if a == 2 {
		print "Richtige Rechnung mit a"
	}
}
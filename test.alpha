main() {
	print " :: Base test:"
	print 42
	print 23

	a = 42
	b = a

	print " :: First variables:"
	print a
	print b

	a = 4
	b = 2

	print " :: Reassigned variables:"
	print a
	print b

	print " :: Math. prints:"
	print 2 + 2 + 2 + 2
	print 2 - 2 - 2 - 2
	print 2 * 2
	print 25 + 5
	print 1 + 2 * 3

	print " :: Math. variables:"
	c = 1 + 2 * 3
	d = (1 + 2) * 3
	e = 2 - 3 * 2
	f = (2 - 3) * 2

	print c
	print d
	print e
	print f

	print " :: Math. prints with variables:"
	print c + d
	print d + c

	print " :: Math. with variables and stored into variables:"

	c = c + d

	print " > c is now ", c
	print " > But d is still ", d

	print " :: Pointer Arithmetik:"
	g -> b
	h <- g

	print g
	print h

	print "Hallo,", "Welt"

	print " :: If Test ohne else aber mit Klammern:"

	if (a) {
		print "A ist valide"
	}

	a = 0

	if (a) {
		print "A ist immer noch valide"
	}

	if (a == 0) {
		print "A ist 0"
	}

	print " :: If Test ohne else und ohne Klammern:"

	a = a + 1
	a = a * 2

	if a == 2 {
		print  "a ist 2"
	}

	if a != 3 {
		print "a ist nicht 3"
	}

	if a > 1 and a < 3 {
		print "a muss 2 sein"
	}

	print "a = ", a

	print " :: If Test mit Else: "

	if a < 2 {
		print "a ist kleiner als 2"
	} else {
		print "a ist nicht kleiner als 2"
	}

	if a > 2 {
		print "a ist mehr als 2"
	} else if a == 2 {
		print "a ist nicht mehr als 2, sondern genau 2!"
	}

	print " :: While Loop Test"

	i = 4
	while (i > 0) {
		i--
		print "i ist jetzt ", i
	}

	print "Und nun ist i = ", i

	print " :: Div, Mod und Mul:"

	a = 24
	b = 4
	c = 5

	print a / b
	print a % c
	print b * a

	j = a / b
	print "div: a = ", a, ", b = ", b, ", j = ", j
	k = a % c
	print "mod: a = ", a, ", c = ", c, ", k = ", k

	if (a % b == 0) {
		print "a ist durch b ohne rest teilbar"
	}

	print "Between 1 and 100 the following prime numbers exist:"

    p = 2
    while p < 100 {
        l = 2
        is_prime = 1

        while l < p {
            if p % l == 0 {
                is_prime = 0
                # print "p ist durch l ohne rest teilbar"
            }

            l++
            #print "l = ", l, " and p = ", p
        }

        if is_prime != 0 {
            print "Prime: ", p
        }

        # print "p = ", p
        p++
    }

    # print "l = ", l # Nothing to print!
    l = 42
    print "l = ", l

	#i, j = 0
	#i = 1
	#i, j = j, i

	say(42)

	#k = fib(12)
	#l = fak(8)
}

say(num) {
	print "Paramater #1 von Function 'say' ist ", num
}